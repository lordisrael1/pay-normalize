{
  "name": "@pay-normalize/example-paystack-ledger",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reference host integration: verify -> parse -> idempotent ingest for Paystack.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p ."
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "6.0.3"
  },
  "dependencies": {
    "@pay-normalize/core": "0.1.1",
    "@pay-normalize/paystack": "0.1.3"
  }
}
