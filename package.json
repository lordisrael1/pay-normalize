{
  "name": "pay-normalize",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:mutation": "stryker run",
    "typecheck": "tsc --noEmit -p packages/core && tsc --noEmit -p packages/nomba && tsc --noEmit -p packages/paystack && tsc --noEmit -p packages/flutterwave && tsc --noEmit -p packages/monnify && tsc --noEmit -p examples/paystack-ledger",
    "check:fixtures": "node scripts/check-fixtures-pii.mjs",
    "check": "npm run check:fixtures && npm run typecheck && npm test"
  },
  "overrides": {
    "qs": "^6.15.2"
  },
  "devDependencies": {
    "@stryker-mutator/core": "9.6.1",
    "@stryker-mutator/vitest-runner": "9.6.1",
    "fast-check": "4.9.0",
    "tsup": "8.5.1",
    "typescript": "6.0.3",
    "vitest": "3.2.7"
  }
}
