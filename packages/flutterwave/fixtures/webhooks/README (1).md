# Flutterwave fixtures — provenance
- `docs.*` = from Flutterwave's v4 documentation samples. EXPERIMENTAL until
  prod.* sanitized captures land. IMPORTANT: donate webhook fixtures WITH
  HEADERS — the docs contradict themselves on the signature scheme
  (HMAC-SHA256-base64 vs plain-secret compare) and only a real capture settles it.
