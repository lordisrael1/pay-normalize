# Nomba webhook fixtures
- `docs.*` = from Nomba's published documentation (the payment_success sample
  is the payload from their signature worked-example, which doubles as the
  golden vector). Replace with `prod.*` captures from Owoore to graduate.
