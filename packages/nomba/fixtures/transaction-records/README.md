# Nomba transaction records — prod.sanitized.*
Sanitized REAL records from Owoore production traffic (names, account numbers,
user/wallet/outlet IDs replaced; structure, field names, money values, and
timestamp formats byte-preserved). These are the corpus's first prod-grade
fixtures and what the money model (amountCharged = amount + fixedCharge) is
pinned against.
